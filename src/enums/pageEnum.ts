export enum PageEnum {
  // basic login path  登录
  BASE_LOGIN = '/login',
  // basic home path   首页
  BASE_HOME = '/dashboard',
  // error page path
  ERROR_PAGE = '/exception',
  // error log page path
  ERROR_LOG_PAGE = '/error-log/list',
}
